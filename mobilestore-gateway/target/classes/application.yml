spring:
  application:
     name: GATEWAY 
  config:
    import: configserver:http://${confighost}:${configport}
eureka:
  client:
    service-url:
      default-zone: http://EUREKA-REGISTRY

     

     




   
    
